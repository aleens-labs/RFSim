window.EW_SIM_CONFIG = {
  apiBaseUrl: window.location.origin + "/api",
  // Intentionally blank in source control. Shared deployments should inject a
  // least-privilege token at container start or provide it through the UI.
  cesiumIonDefaultToken: ""
};
