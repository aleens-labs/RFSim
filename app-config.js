window.EW_SIM_CONFIG = {
  apiBaseUrl: window.location.origin + "/api",
  // Keep shared deployments blank. Operators and users should provide Cesium
  // tokens through the UI so credentials are not published in app-config.js.
  cesiumIonDefaultToken: ""
};
